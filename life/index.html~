---
layout: page
---
<div class="list_nav">
				<h1>Blog</h1>
				<div class="list">
					<ul>
						{% for post in site.posts %}
                                		<li><a href="{{ post.url }}">{{ post.title }}</a></li>
                                		{% endfor %}
					</ul><!-- / -->
				</div>

				<h1>Life</h1>
				<div class="list">
					<ul>
						{% for post in site.posts %}
                                		<li>
                                      		<h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
                                		</li>
                                		{% endfor %}
					</ul><!-- / -->
				</div>
			</div>

		<div class="page">
			<ul class="artical">
				{% for post in site.posts %}
                                <li>
                                      <h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
                                      <p>{{ post.date }}</p>
                                 {{ post.description }}
                                </li>
                                {% endfor %}
					
					
				</ul><!-- / -->
			</div>
